CREATE TABLE CLIENTE (
    ID_CLIENTE SERIAL PRIMARY KEY,
    NOME VARCHAR(255) NOT NULL,
    SENHA VARCHAR(255) NOT NULL,
    CPF VARCHAR(50) NOT NULL UNIQUE,
    TELEFONE VARCHAR(50),
    ENDERECO VARCHAR(255)
);

CREATE TABLE VEICULO (
    ID_VEICULO SERIAL PRIMARY KEY,
    MODELO VARCHAR(255) NOT NULL,
    COR VARCHAR(255) NOT NULL,
    PLACA VARCHAR(255) NOT NULL,
    ANO INT,
    MARCA VARCHAR(255) NOT NULL,
    ID_CLIENTE INT REFERENCES CLIENTE(ID_CLIENTE)
);

CREATE TABLE MANUTENCAO (
    ID_MANUTENCAO SERIAL PRIMARY KEY,
    DATA_ENT DATE,
    PREVISAO_SAIDA DATE NOT NULL,
    SITUACAO VARCHAR(255),
    DESCRICAO_PROBLEMA VARCHAR(3000),
    TIPO_SERVICO VARCHAR(300),
    VALOR DECIMAL(10,2),
    ID_VEICULO INT REFERENCES VEICULO(ID_VEICULO)
);

INSERT INTO CLIENTE (NOME, SENHA, CPF, TELEFONE, ENDERECO)
VALUES 
    ('Rodrigo', '12345678', '44444444441', '14999999999', 'R. Carlos Bray'),
    ('Bruno',   '12345678', '44444444442', '14999999999', 'R. Carlos Bray'),
    ('Michele', '12345678', '44444444443', '14999999999', 'R. Carlos Bray');

SELECT * FROM CLIENTE;

INSERT INTO VEICULO (MODELO, COR, PLACA, ANO, MARCA, ID_CLIENTE)
VALUES 
    ('Gol',  'Preto', 'GLX-6666', 1996, 'Volkswagen', 1),
    ('Fox',  'Preto', 'GLX-6667', 2010, 'Volkswagen', 2),
    ('Polo', 'Preto', 'GLX-6668', 2020, 'Volkswagen', 3);

SELECT * FROM VEICULO;

INSERT INTO MANUTENCAO (PREVISAO_SAIDA, DESCRICAO_PROBLEMA, TIPO_SERVICO, VALOR, ID_VEICULO)
VALUES 
    ('2026-09-30', 'Capô amassado', 'Funilaria', 6000.00, 1),
    ('2026-09-20', 'Amortecedores estourados', 'Suspensão', 6000.00, 2);

SELECT m.*,v.modelo, v.marca, v.cor, v.ano, v.id_cliente, c.nome, c.telefone,
c.endereco FROM manutencao AS m JOIN veiculo AS v ON m.id_veiculo = v.id_veiculo
JOIN cliente AS c ON v.id_cliente = c.id_cliente;

SELECT * FROM MANUTENCAO;